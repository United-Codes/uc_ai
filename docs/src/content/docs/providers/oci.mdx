---
title: OCI
description: UC AI provider documentation for Oracle Cloud Infrastructure
---

The UC AI OCI package provides integration with Oracle Cloud Infrastructure's Generative AI models, allowing you to use state-of-the-art language models within your Oracle database applications.

## Features

- Support for Cohere, Meta Llama, xAI Grok models, and Google Gemini models
- Features vary by model

## Responses API

As of v26.2, UC AI routes OCI calls through OCI's Responses API endpoint by default for models that support it. To fall back to the generic chat endpoint, set:

```sql
uc_ai_oci.g_use_responses_api := false;
```

## Max output tokens

OCI chat requests are capped by `maxTokens`, which defaults to `4096`. Raise it for long outputs (e.g. document extraction) to avoid silent truncation:

```sql
uc_ai_oci.g_max_tokens := 8192;
```

When the model stops because this cap was reached, UC AI reports `finish_reason = 'length'` so callers can detect truncation. (Can also be set per call via the provider config key `g_max_tokens`.)

## Prerequisites

1. Create OCI API Key and APEX Web Credential (see [guide](/products/uc-ai/docs/help/create-oci-key/))
2. Oracle database with internet access to OCI's API endpoints (see [Network Setup (ACL & Wallet)](/products/uc-ai/docs/guides/network-setup/))
3. UC AI package installed

Note that OCI uses a complex credential-based authentication system with a private key and three other components rather than a simple API key. Unfortunately Oracle does not provide a PL/SQL SDK to do the cryptographic operations, so the UC AI package builds on Oracle APEX Web Credentials which can do the work.

If you have a way to do the cryptographic operations in PL/SQL, please contact me or open a pull request to improve allow this to work without having to use APEX. Also if you know a way to get `Auth tokens` (simple API keys) to work with the AI APIs, please let me know.

## Models

Which models are available to you is highly dependent on the region where your OCI tenant is located.

Currently Oracle provides Cohere, Meta Llama, xAI Grok models, and Google Gemini models.

Refer to [this list of available models](https://docs.oracle.com/en-us/iaas/Content/generative-ai/pretrained-models.htm) for more information. Click on a model to see where it is available.

### Meta Llama

- `uc_ai_oci.c_model_llama_4_scout`
- `uc_ai_oci.c_model_llama_4_maverick`
- `uc_ai_oci.c_model_llama_3_3_70b`

### Cohere

- `uc_ai_oci.c_model_cohere_command_a_reasoning`
- `uc_ai_oci.c_model_cohere_command_a_vision`
- `uc_ai_oci.c_model_cohere_command_a_03_2025`

### xAI Grok

- `uc_ai_oci.c_model_grok_4_3`
- `uc_ai_oci.c_model_grok_4_20`
- `uc_ai_oci.c_model_grok_4_20_multi_agent`

### OpenAI

- `uc_ai_oci.c_model_gpt_oss_120b`
- `uc_ai_oci.c_model_gpt_oss_20b`

### Google Gemini

- `uc_ai_oci.c_model_google_gemini_2_5_pro`
- `uc_ai_oci.c_model_google_gemini_2_5_flash`
- `uc_ai_oci.c_model_google_gemini_2_5_flash_lite`

### Retired Models

Oracle marks the Grok 3 and Grok 4 models, `grok-code-fast-1`, the Llama 3.1 and Llama 3.2 models, and the Command R models as deprecated. The same applies to the Cohere embedding models of version 3. The package keeps their constants, but use a current model for new code.

:::tip
If a new model is not yet available as a constant, you can pass the model name directly as a string. See the [FAQ](/products/uc-ai/docs/other/faq/#custom-model-strings) for details.
:::

## Usage Examples

### Basic Text Generation

```sql
declare
  l_result json_object_t;
begin
  uc_ai_oci.g_compartment_id := 'your_compartment_id_here';
  uc_ai_oci.g_region := 'eu-frankfurt-1';
  uc_ai_oci.g_apex_web_credential := 'NAME_OF_YOUR_APEX_WEB_CREDENTIAL';

  -- optional
  uc_ai_oci.g_serving_type := 'ON_DEMAND'; -- ON_DEMAND or DEDICATED, default is ON_DEMAND

  l_result := uc_ai.generate_text(
    p_user_prompt => 'What is Oracle APEX?',
    p_provider => uc_ai.c_provider_oci,
    p_model => uc_ai_oci.c_model_cohere_command_a_03_2025
  );

  dbms_output.put_line('AI Response: ' || l_result.get_string('final_message'));
end;
/
```

### With System Prompt

```sql
declare
  l_result json_object_t;
begin
  uc_ai_oci.g_compartment_id := 'your_compartment_id_here';
  uc_ai_oci.g_region := 'eu-frankfurt-1';
  uc_ai_oci.g_apex_web_credential := 'NAME_OF_YOUR_APEX_WEB_CREDENTIAL';

  -- optional
  uc_ai_oci.g_serving_type := 'ON_DEMAND'; -- ON_DEMAND or DEDICATED, default is ON_DEMAND

  l_result := uc_ai.generate_text(
    p_user_prompt => 'I have tomatoes, salad, potatoes, olives, and cheese. What can I cook with that?',
    p_system_prompt => 'You are an assistant helping users to get recipes. Please just list 3 possible dish names without instructions.',
    p_provider => uc_ai.c_provider_oci,
    p_model => uc_ai_oci.c_model_cohere_command_a_03_2025
  );

  dbms_output.put_line('Recipe suggestions: ' || l_result.get_string('final_message'));
end;
/
```

### Tools

Tools / function calling is supported. Refer to the [model documentation page](https://docs.oracle.com/en-us/iaas/Content/generative-ai/pretrained-models.htm) to see which models support it.

### File input (images & PDFs)

OCI supports file input through the [file analysis](/products/uc-ai/docs/guides/file_analysis/) message API, but only via its GENERIC chat format:

- Use a GENERIC-format vision model (Meta Llama vision, Google Gemini, xAI Grok). **Cohere models reject file content.**
- **Images:** PNG or JPG only, ≤ 5 MB.
- **Documents:** PDF only.
- Other types (e.g. WebP, GIF) are rejected with `ORA-20303`.

## Generating Embeddings

OCI provides Cohere embedding models for semantic search, clustering, and other vector-based operations:

- `uc_ai_oci.c_model_cohere_embed_4` - Latest Cohere v4 embedding model
- `uc_ai_oci.c_model_cohere_embed_english_3` - English-optimized v3 model
- `uc_ai_oci.c_model_cohere_embed_english_light_3` - Lightweight English model
- `uc_ai_oci.c_model_cohere_embed_multi_3` - Multilingual v3 model
- `uc_ai_oci.c_model_cohere_embed_multi_light_3` - Lightweight multilingual model

Image-capable embedding models are also available:

- `uc_ai_oci.c_model_cohere_embed_english_image_3`
- `uc_ai_oci.c_model_cohere_embed_english_light_image_3`
- `uc_ai_oci.c_model_cohere_embed_multi_image_light_3`

Also refer to the [OCI documentation](https://docs.oracle.com/en-us/iaas/Content/generative-ai/pretrained-models.htm) for any new models that might not be listed here and region availability.

```sql
declare
  l_result json_array_t;
begin
  uc_ai_oci.g_compartment_id := 'your_compartment_id_here';
  uc_ai_oci.g_region := 'eu-frankfurt-1';
  uc_ai_oci.g_apex_web_credential := 'NAME_OF_YOUR_APEX_WEB_CREDENTIAL';

  l_result := uc_ai.generate_embeddings(
    p_input => json_array_t('["Oracle APEX is a low-code development platform.", "UC AI allows you to easily use AI from PL/SQL."]'),
    p_provider => uc_ai.c_provider_oci,
    p_model => uc_ai_oci.c_model_cohere_embed_english_3
  );

  -- l_result.get_size → 2 (one embedding per input string)
  -- l_result.get(0) → JSON array of embedding values for first input
end;
/
```

For more information about the `generate_embeddings` function, visit its [documentation page](/products/uc-ai/docs/api/generate_embeddings/).
